{
  "name": "mytimeoff",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@10.7.1",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10.7.1"
  },
  "scripts": {
    "reader": "pnpm --filter @mytimeoff/reader dev",
    "bundle:cli": "node scripts/bundle-cli.mjs",
    "test:installer-hooks": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test-installer-hooks.ps1",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "~2.11.0"
  }
}
